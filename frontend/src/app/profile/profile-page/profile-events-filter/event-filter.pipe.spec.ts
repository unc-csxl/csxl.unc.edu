import { ProfileEventsFilterPipe } from './event-filter.pipe';

describe('ProfileEventsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileEventsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
