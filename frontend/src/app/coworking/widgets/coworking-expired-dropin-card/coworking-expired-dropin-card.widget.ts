import { Component, Input } from '@angular/core';
import { Profile } from '../../../models.module';

@Component({
    selector: 'coworking-expired-dropin-card',
    templateUrl: './coworking-expired-dropin-card.widget.html',
    styleUrls: ['./coworking-expired-dropin-card.widget.css']
})
export class CoworkingExpiredDropInCard {

    constructor() { }
}